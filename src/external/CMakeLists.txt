INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})

SET(FILES_HXX)
SET(FILES_CPP)

SET(FILES_HXX ${FILES_HXX}
    PARENT_SCOPE
)

SET(FILES_CPP ${FILES_CPP}
    PARENT_SCOPE
)
SET(MODULES ${MODULES} PARENT_SCOPE)

